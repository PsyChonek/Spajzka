{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["build/**"],
      "env": ["NODE_ENV", "PUBLIC_URL", "CI=false", "REACT_APP_SpajzkaAPI"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "generateClient":{
      
    }
  }
}