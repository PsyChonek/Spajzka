# API Configuration
# For development, point to the API server
VITE_API_URL=http://localhost:3000

# MCP Configuration
# Public URL shown to users on the Profile → MCP access card.
# Users paste this into their MCP client (Claude Desktop, Claude.ai, etc).
VITE_MCP_PUBLIC_URL=http://localhost:3001/mcp
